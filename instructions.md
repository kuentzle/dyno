# Environment & Terminal Rules
- **OS:** Windows
- **Terminal:** PowerShell (Version < 7)
- **Constraint:** The `&&` operator is NOT supported in this terminal.
- **Requirement:** Always use `;` to chain commands or provide them on separate lines.
- **Preferred Syntax:** `npm run command1 ; npm run command2`